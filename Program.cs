using Microsoft.EntityFrameworkCore.Metadata.Internal;
using System;

namespace Critical_Thinking_Loops
{
    class Program
    {
        string numbers = "Counter";
        for (int index = 9; index< number.length; Index++ )
       
                                      
        {
            Console.WriteLine(index);
        }
    }
}
